% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script-deps.R
\name{check_script_deps}
\alias{check_script_deps}
\title{Check and install missing dependencies for chapter scripts}
\usage{
check_script_deps(lecture, chapter, pattern = "[.]R$", install = interactive())
}
\arguments{
\item{lecture}{Character. Lecture directory name, e.g. \code{"lecture_i2ml"}.}

\item{chapter}{Character. Chapter directory name, e.g. \code{"evaluation"}.}

\item{pattern}{Regex pattern to filter scripts. Default \code{"[.]R$"}.}

\item{install}{Logical. If \code{TRUE} (default in interactive sessions),
prompt to install missing packages. If \code{FALSE}, only report.}
}
\value{
Invisibly: A list with \code{all} (all detected packages) and
\code{missing} (packages not currently installed).
}
\description{
Extracts package dependencies from all R scripts in a chapter's \verb{rsrc/}
directory, checks which are not installed, and offers to install them
via \code{\link[pak:pak]{pak::pak()}}.
}
\examples{
\dontshow{if (fs::dir_exists(here::here("lecture_i2ml"))) withAutoprint(\{ # examplesIf}
check_script_deps("lecture_i2ml", "evaluation", install = FALSE)
\dontshow{\}) # examplesIf}
}
