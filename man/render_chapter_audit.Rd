% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-audit.R
\name{render_chapter_audit}
\alias{render_chapter_audit}
\title{Render a chapter audit report}
\usage{
render_chapter_audit(
  lecture_dir = ".",
  chapters = NULL,
  pattern = "[.]R$",
  timeout = 300,
  run = FALSE,
  output_dir = lecture_dir,
  ...
)
}
\arguments{
\item{lecture_dir}{Character. Path to the lecture directory.
Defaults to the current working directory.}

\item{chapters}{Character vector of chapter names to audit, or \code{NULL}
(default) to audit all chapters.}

\item{pattern}{Regex pattern to filter script filenames.
Default \code{"[.]R$"} matches all \code{.R} files.}

\item{timeout}{Numeric. Per-script timeout in seconds. Default 300.}

\item{run}{Logical. If \code{TRUE}, execute scripts and track produced figures.
Default \code{FALSE}.}

\item{output_dir}{Character. Directory for the output HTML file.
Defaults to \code{lecture_dir}.}

\item{...}{Additional arguments passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}.}
}
\value{
The path to the rendered HTML file (invisibly), as returned by
\code{\link[rmarkdown:render]{rmarkdown::render()}}.
}
\description{
Renders an HTML report auditing the script-figure-slide dependency chain
for a lecture. Uses the \code{chapter_audit.Rmd} template bundled with the
package.
}
\examples{
\dontrun{
# From within a lecture directory (e.g. lecture_i2ml/)
render_chapter_audit()

# From lecture_service root
render_chapter_audit(lecture_dir = "lecture_i2ml")

# Audit specific chapters with script execution
render_chapter_audit(
  chapters = c("cart", "evaluation"),
  run = TRUE
)
}
}
